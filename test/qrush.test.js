describe("QRush", function () {
	let qRush;

	beforeEach(function () {
		qRush = QRush();
	});

	describe("functionName", function () {
		it("description of test", function () {
			assert(qRush.functionName(), true);
		});
	});
});